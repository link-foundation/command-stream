---
bump: patch
---

### Fixed
- Isolate virtual `cd` changes to one command or pipeline invocation, including overlapping commands and explicit working directories, then restore the host working directory and `PWD`/`OLDPWD`.
